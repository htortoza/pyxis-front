/** Horizontal, cross-cutting reference dimensions -- not part of the org containment tree. */
export interface Marca {
  id: string;
  label: string;
}

export interface Sector {
  id: string;
  label: string;
}
